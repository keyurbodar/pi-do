export function PrListPage() {
  return (
    <section aria-label="PR list">
      <h1>PRs</h1>
      <p>PR list shell. GitHub fetch plugs in next phase.</p>
    </section>
  );
}

export function PrViewerPage() {
  return (
    <section aria-label="PR viewer">
      <h1>PR viewer</h1>
      <p>Single PR shell: files patch plus comments.</p>
    </section>
  );
}
